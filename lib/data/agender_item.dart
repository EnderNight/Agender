class AgenderItem {
  String title;
  String description;
  DateTime dueDate;
  bool done;

  AgenderItem({
    required this.title,
    required this.description,
    required this.dueDate,
    required this.done,
  });
}
